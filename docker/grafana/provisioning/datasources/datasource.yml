apiVersion: 1

# Auto-provision Prometheus as the default datasource so no manual setup is needed.
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
