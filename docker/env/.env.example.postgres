# pgvector (PostgreSQL) service credentials. Copy to .env.postgres.
# POSTGRES_USER / POSTGRES_PASSWORD / POSTGRES_DB must match the values
# the backend uses in .env.app (POSTGRES_USERNAME / POSTGRES_PASSWORD / POSTGRES_DB).
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me
POSTGRES_DB=minirag-v1
