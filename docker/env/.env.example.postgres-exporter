# postgres-exporter config. Copy to .env.postgres-exporter.
# Credentials must match the pgvector service. host=pgvector, the compose service name.
DATA_SOURCE_NAME=postgresql://postgres:change_me@pgvector:5432/minirag-v1?sslmode=disable
